---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gpu-config-plugin
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/rhpds/gpu-resource-mgmt-automation
    targetRevision: gb300
    path: workloads/gpu-config-plugin
  destination:
    server: https://kubernetes.default.svc
    namespace: gpu-config-plugin
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m